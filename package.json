{
  "name": "finance-tracker",
  "private": true,
  "engines": {
    "node": "24.15.0",
    "pnpm": "10.33.0"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=@acme/api --filter=@acme/web",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "typecheck": "turbo run typecheck",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f db",
    "db:generate": "turbo run db:generate --filter=@acme/api",
    "db:migrate": "turbo run db:migrate --filter=@acme/api",
    "db:push": "turbo run db:push --filter=@acme/api",
    "db:studio": "turbo run db:studio --filter=@acme/api"
  },
  "devDependencies": {
    "turbo": "2.9.7",
    "typescript": "catalog:"
  }
}
